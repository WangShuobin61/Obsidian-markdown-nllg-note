### ✍ 开发 + 提交（各自在自己的 feature 上）

```bash
git status     //查看当前git状态
git add .
git commit -m "feat: 实现雷达视图基础显示"
```

**提交规范建议（很重要）**

- `feat:` 新功能
- `fix:` 修 bug
- `refactor:` 重构
- `ui:` 纯 UI 改动
- `chore:` 杂项